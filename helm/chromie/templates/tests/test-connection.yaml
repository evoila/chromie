apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chromie.fullname" . }}-test-connection"
  labels:
    {{- include "chromie.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chromie.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
