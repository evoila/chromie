apiVersion: v1
kind: Service
metadata:
  name: {{ include "chromie.fullname" . }}
  labels:
    {{- include "chromie.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chromie.selectorLabels" . | nindent 4 }}
